server {
    listen 80;
    server_name gextimoapi.novafriq.africa;
    root /var/www/gextimo_backend/public;
    index index.php index.html;

    client_max_body_size 50M;

    # Bundles OTA (@capgo/capacitor-updater), stockés hors du public/.
    # ⚠️ Indispensable : sans ça les mises à jour à chaud ne se téléchargent pas.
    location /app-bundles/ {
        alias /var/www/app-bundles/;
        add_header Cache-Control "public, max-age=3600";
    }

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location ~ \.php$ {
        include snippets/fastcgi-php.conf;
        fastcgi_pass unix:/run/php/php8.4-fpm.sock;
        fastcgi_param SCRIPT_FILENAME $realpath_root$fastcgi_script_name;
        include fastcgi_params;
        fastcgi_read_timeout 300;
    }

    location ~ /\.(?!well-known).* {
        deny all;
    }

    error_log  /var/log/nginx/gextimo_error.log;
    access_log /var/log/nginx/gextimo_access.log;
}
